apiVersion: v1
kind: Service
metadata:
  name: retriever
  labels:
    app: retriever
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: 50051
      name: grpc
  selector:
    app: retriever
